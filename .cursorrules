# Employee Nexus Frontend - Coding Standards

## 🎯 Project Overview
This is a Next.js 14+ React application using TypeScript, GraphQL, and modern UI libraries.

## 📐 Naming Conventions

### TypeScript Interfaces & Types
- **ALWAYS** prefix interface names with `I`
  - ✅ `interface IUserProps { ... }`
  - ✅ `interface IEmployee { ... }`
  - ❌ `interface UserProps { ... }`
  
- **ALWAYS** prefix type names with `I` when they represent object shapes
  - ✅ `type IFormData = { ... }`
  - ✅ `type ITableConfig = { ... }`

### Component Props
- Use `I` prefix for props interfaces
  - ✅ `interface ICustomTableProps { ... }`
  - ✅ `interface IPageHeaderProps { ... }`

### Files & Folders
- Components: PascalCase (e.g., `CustomTable.tsx`, `UserProfileCell.tsx`)
- Utilities: camelCase (e.g., `formatDate.ts`, `validateEmail.ts`)
- Constants: UPPER_SNAKE_CASE files (e.g., `API_ENDPOINTS.ts`)

## 🎬 Animation Standards

### Primary Animation Library
- **REQUIRED**: Use **Framer Motion** for all animations
  - ✅ `import { motion } from "framer-motion"`
  - ❌ Do NOT use CSS animations for complex interactions
  - ❌ Do NOT use other animation libraries (anime.js, GSAP, etc.)

### When to Use Framer Motion
- ✅ Page transitions
- ✅ Component enter/exit animations
- ✅ Skeleton loaders
- ✅ Modal/dialog animations
- ✅ List stagger animations
- ✅ Hover/tap interactions that need orchestration

### Simple CSS Animations (Allowed)
- ✅ Simple hover states (`:hover` pseudo-class)
- ✅ Tailwind's `transition` utilities for basic transitions
- ✅ `animate-spin`, `animate-pulse` for simple effects

### Animation Best Practices
```tsx
// ✅ GOOD: Use variants for reusable animations
const containerVariants = {
  hidden: { opacity: 0 },
  visible: {
    opacity: 1,
    transition: { staggerChildren: 0.1 }
  }
};

// ✅ GOOD: Semantic variant names
<motion.div variants={cardVariants} initial="hidden" animate="visible">

// ❌ BAD: Inline animation objects
<motion.div animate={{ opacity: 1 }} initial={{ opacity: 0 }}>
```

## 🎨 Styling Standards

### CSS Framework
- **Primary**: Tailwind CSS
- **Component Library**: DaisyUI (built on Tailwind)

### Gradient Classes
- Use `bg-linear-to-r`, `bg-linear-to-br` instead of `bg-gradient-to-r`
  - ✅ `className="bg-linear-to-br from-blue-50 to-blue-100"`
  - ❌ `className="bg-gradient-to-br from-blue-50 to-blue-100"`

### Custom Components
- Create reusable components for repeated UI patterns
- Examples: `UserProfileCell`, `StatusBadge`, `PageHeader`

## 📝 Code Documentation

### Comments Style
- Use **UPPERCASE COMMENTS** for major sections
  - ✅ `// ==================== GRAPHQL QUERY: FETCH USERS ====================`
  - ✅ `// CUSTOM USER PROFILE COLUMN WITH AVATAR AND DESIGNATION`

### Function Documentation
- Add JSDoc comments for complex functions
- Document props interfaces with comments

## 🏗️ Component Structure

### Standard Component Pattern
```tsx
"use client"; // For Next.js client components

import { ... } from "...";

// ==================== INTERFACES ====================
interface IComponentProps {
  // ... props
}

// ==================== COMPONENT ====================
export default function ComponentName({ ...props }: IComponentProps) {
  // ==================== HOOKS ====================
  const [state, setState] = useState();
  
  // ==================== HANDLERS ====================
  const handleClick = () => { ... };
  
  // ==================== RENDER ====================
  return (
    <div>...</div>
  );
}
```

## 🔄 State Management

### GraphQL
- Use Apollo Client hooks (`useQuery`, `useMutation`)
- Always type GraphQL responses
```tsx
const { data, loading } = useQuery<{
  users: {
    data: IUser[];
    meta: IMeta;
  };
}>(GET_USERS);
```

### Local State
- Use `useState` for component state
- Use custom hooks for reusable logic
- Prefix custom hooks with `use` (e.g., `usePermissionGuard`, `usePopupOption`)

## 📦 File Organization

### Import Order
1. React & Next.js imports
2. External libraries
3. Internal components
4. Types/Interfaces
5. Constants
6. Utilities
7. Styles

```tsx
// 1. React/Next
import { useState } from "react";
import { useRouter } from "next/navigation";

// 2. External libraries
import { useQuery } from "@apollo/client";
import { motion } from "framer-motion";

// 3. Components
import CustomTable from "@/components/table/CustomTable";
import PageHeader from "@/components/ui/PageHeader";

// 4. Types
import type { IEmployee, ITableConfig } from "@/types";

// 5. Constants
import { Permissions } from "@/constants/permissions.constant";

// 6. GraphQL
import { GET_USERS } from "@/graphql/user.api";
```

## 🎯 GraphQL Conventions

### Query/Mutation Naming
- Use UPPER_SNAKE_CASE for queries and mutations
  - ✅ `GET_USERS`, `CREATE_EMPLOYEE`, `UPDATE_USER_STATUS`
  - ❌ `getUsers`, `createEmployee`

### File Organization
- Group by domain: `user.api.ts`, `employee.api.ts`, `department.api.ts`

## ✅ Best Practices

### Type Safety
- **NEVER** use `any` unless absolutely necessary
- Prefer `unknown` over `any` when type is uncertain
- Use type guards for runtime type checking

### Error Handling
- Always handle GraphQL errors
- Use try-catch for async operations
- Provide user feedback via toast notifications

### Accessibility
- Use semantic HTML elements
- Add proper ARIA labels
- Ensure keyboard navigation works
- Maintain proper heading hierarchy

### Performance
- Use `useMemo` for expensive computations
- Use `useCallback` for memoized callbacks
- Implement proper loading states
- Use skeleton loaders (with Framer Motion) instead of spinners

## 🚫 Anti-Patterns to Avoid

### Naming
- ❌ Interfaces without `I` prefix
- ❌ Magic numbers (use named constants)
- ❌ Unclear variable names (`data`, `temp`, `x`)

### Code Structure
- ❌ Inline styles (use Tailwind classes)
- ❌ Deeply nested ternaries (extract to variables/functions)
- ❌ Large component files (>500 lines - consider splitting)

### State Management
- ❌ Prop drilling (use context or composition)
- ❌ Mutating state directly
- ❌ Storing derived state

## 🔧 Tools & Configuration

### Required Dev Tools
- TypeScript
- ESLint
- Prettier
- Tailwind CSS IntelliSense

### Code Formatting
- Run `prettier` before committing
- Follow existing `.prettierrc` configuration
- Use 2 spaces for indentation

## 📚 Component Libraries

### Allowed
- ✅ Framer Motion (animations)
- ✅ React Icons (icons)
- ✅ DaisyUI (UI components)
- ✅ Tailwind CSS (styling)

### Avoid
- ❌ Other animation libraries
- ❌ Other CSS frameworks
- ❌ jQuery or legacy libraries

## 🎨 UI/UX Standards

### Loading States
- Use skeleton loaders with Framer Motion
- Match skeleton structure to actual content
- Implement stagger animations for lists

### Forms
- Use controlled components
- Implement proper validation
- Show clear error messages
- Provide loading states for submissions

### Tables
- Use `CustomTable` component
- Implement proper loading states
- Support mobile responsive card view
- Include search and filtering when applicable

---

## 📋 Quick Reference

**When creating a new component:**
1. ✅ Props interface starts with `I`
2. ✅ Use Framer Motion for animations
3. ✅ Add uppercase section comments
4. ✅ Follow component structure pattern
5. ✅ Use semantic HTML
6. ✅ Implement loading states

**When adding animations:**
1. ✅ Import `motion` from Framer Motion
2. ✅ Define variants for reusability
3. ✅ Use stagger for lists
4. ✅ Keep animations subtle and professional
5. ✅ Use `easeOut` for entrances, `easeInOut` for loops

**When writing interfaces:**
1. ✅ Always use `I` prefix
2. ✅ Document complex props with comments
3. ✅ Use optional `?` sparingly, prefer defaults
4. ✅ Export from main types file when shared

---

*These rules ensure consistency, maintainability, and professional quality across the codebase.*
